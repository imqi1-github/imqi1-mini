# 复制为 .env.development（开发）和 .env.production（生产）后填入真实地址
# 此文件仅作模板，会被提交；.env.* 实际文件被 gitignore 忽略
#
# 构建期按 mode 内联对应文件，互不携带：
#   uni dev → .env.development
#   uni build → .env.production（腾讯审核 / 正式发布用）

# 后端 API 基址，统一包含 /api/mini
#   dev  示例：http://localhost:3000/api/mini
#   prod 示例：https://imqi1.com/api/mini（须 HTTPS）
VITE_API_BASE_URL=https://imqi1.com/api/mini

# 小程序 API 签名密钥，须与后端 MINI_API_SECRET 完全一致。
# 用于对 /api/mini/* 请求做 HMAC-SHA256 签名（防 curl 直连刷接口 / 评论机器人）。
# 留空则请求不带签名头；后端未配置密钥时也不校验，二者需同步开关。
VITE_MINI_API_SECRET=
