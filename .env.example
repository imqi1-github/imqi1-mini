# 小程序端环境变量模板
# ─────────────────────────
# 用法：
#   1) 复制本文件为 .env.development（开发联调用）与 .env.production（正式构建用）
#   2) 把下面的 https://example.com 改成你自己的后端域名
#   3) VITE_MINI_API_SECRET 必须与主站根目录 .env 中的 MINI_API_SECRET 完全一致
#      （主站 .env.example 同样有 MINI_API_SECRET 这一行）；任一端留空都会让接口 401
# 4) .env.development / .env.production 已在 .gitignore 中（.env.* 通配 + !.env.example 例外），
#    请勿将含真实密钥的版本提交进仓库

# 后端 API 基址，统一包含 /api/mini
# - 开发联调指向本地主站（端口与主站 nuxt.config 一致）：
#     VITE_API_BASE_URL=http://localhost:3000/api/mini
# - 正式构建指向生产域名，必须 HTTPS：
#     VITE_API_BASE_URL=https://example.com/api/mini
VITE_API_BASE_URL=https://example.com/api/mini

# 小程序 API 签名密钥，须与后端 MINI_API_SECRET 完全一致
# 留空时客户端不发签名头，开发环境两端可以同时留空用于联调；
# 生产环境两端都必须填写，否则服务端 mini-auth 中间件会拒绝所有请求。
VITE_MINI_API_SECRET=